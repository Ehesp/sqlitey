# Used when Bun serves static assets in dev. Standalone `bun build --compile` does not read this for bundling;
# release binaries use `scripts/release.ts` (Bun.build + bun-plugin-tailwind) instead.
[serve.static]
plugins = ["bun-plugin-tailwind"]
env = "BUN_PUBLIC_*"
